{
  "name": "lanchonete",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de pedidos para lanchonete — spec v3.1",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "api": "npm run --workspace apps/api",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "npm run test:e2e -w apps/api"
  }
}
